# Configuration Netlify — régénère data.json depuis l'export Semrush au déploiement.
[build]
  publish = "."
  command = "python3 scripts/build_from_csv.py"

[[headers]]
  for = "/data.json"
  [headers.values]
    Cache-Control = "public, max-age=300, must-revalidate"
